collection:

  # Route
  - pattern: "/foo/bar"
    pipeline:

      - type: tcp
        address: golang.org:80

  # Route
  - pattern: "{*}"
    pipeline:

      - type: process
        cmd: python ttt.py
