urlconf:

  /myip:
    - type: socket
      address: tcp://geoiphost/foo/bar

  *:
    - type: process:
      name: "echo -n 'No answer'"
      foo: bar




======




urlconf:

  - case:
    pattern: "myip"
    pipeline:
      - socket:
        address: hostname
      - process:
        name: "echo -n 192.168.1.1"

  - case:
    pattern: "*"
    pipeline:
      - process:
        name: "echo -n 'No answer'"
